import { Button } from "@/components/ui/button"
import { prisma } from "@/lib/db"

const page = async () => {

  return (
    <div>
      <Button>Click me</Button>
    </div>
  )
}
export default page